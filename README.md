# Pygfx and Open3d Example

An example repository to display the issues when converting open3d triangle models to pygfx geometries.

### Installation

```bash
pip install -r requirements.txt
```

### Run

```
python main.py
```